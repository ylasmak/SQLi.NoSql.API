@model SQLi.NoSql.API.MongoR.Lib.Model.Graph

@{
    // TODO: use the data from the model to draw a chart

    var myChart = new Chart(width: Model.width, height: Model.Height)
        .AddTitle(Model.Title)
        .AddSeries(
                 
            name: "Employee",
            xValue: Model.XfiledList,
            yValues: Model.Xvalue)
        .Write();
}

